@{
    ViewData["Title"] = "User Roles List";
}
@model IEnumerable<EmployeeDepartmentCRUDApp.Models.UserRole>

<div class="container-fluid">
    <div class="row align-items-center mb-3">
        <div class="col-md-6">
            <h2 class="text-danger">User Roles List</h2>
        </div>
        <div class="col-md-6 text-md-end mt-2 mt-md-0">
            <a class="btn btn-primary btn-sm me-2" asp-controller="UserRole" asp-action="Create">
                <i class="bi bi-plus-circle"></i> Create User
            </a>
            <a class="btn btn-info btn-sm" asp-controller="UserRole" asp-action="Export">
                <i class="bi bi-download"></i> Export
            </a>
        </div>
    </div>
    <hr />
</div>

<div class="table-responsive">
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th scope="col">Sr.No</th>
                <th scope="col">Username</th>
                <th scope="col">Role</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var userRole in Model.Select((entry, index) => new { entry, index }))
            {
                <tr>
                    <td>@(userRole.index + 1)</td>
                    <td>@userRole.entry.User?.Username</td>
                    <td>@userRole.entry.Role?.Name</td>
                    <td>
                        <a class="btn btn-warning btn-sm mx-2" asp-controller="UserRole" asp-action="Edit" asp-route-id="@userRole.entry.Id">Edit</a>
                        <a class="btn btn-danger btn-sm" asp-controller="UserRole" asp-action="Delete" asp-route-id="@userRole.entry.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>